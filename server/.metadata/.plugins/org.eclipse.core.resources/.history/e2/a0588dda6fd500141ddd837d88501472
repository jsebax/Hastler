package rest;

import java.util.List;

public interface ServiService {
	ServiDTO create(ServiDTO todo);
	 
	ServiDTO delete(String id);
 
    List<ServiDTO> findAll();
 
    ServiDTO findById(String id);
 
    ServiDTO update(ServiDTO todo);
}
